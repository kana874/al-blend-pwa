# リリースノート

## Ver.1.4.0 - 2026-08-24

- 指定された標準配合プリセット19種を追加。
- Cu: 025C / 03C / 05C / 1C / 2C / 4C / 8C。
- Cu+Si: 02S05C / 075S05C / 08S03C / 08S05C / 1S004C / 1S05C。
- Si: 03S / 05S / 08S / 1S / 1.2S / 30ppmS。
- 標準プリセットは純Cu/純Si添加材、歩留まり100%、1 g天秤、四捨五入、溶湯量単位kgを使用。既存端末で1 g天秤が無効・削除済みの場合は天秤を強制変更しない。
- 既存の同名ユーザープリセットは保持し、標準プリセットで上書きしない。
- 既存端末にも一度だけ追加するデータマイグレーションを実装。

## Ver.1.3.0 - 2026-08-24

- 計算履歴のCSV復元に対応。
- Ver.1.2.0以前にアプリから出力した「日時 / 種類 / 概要 / データJSON」形式の履歴CSVを読み込み可能。
- CSVの日時・種類・JSON形式を保存前に検証。
- 同一内容の履歴は重複登録せずスキップ。
- 既存履歴は保持し、新規履歴のみ追加する非破壊復元。

## Ver.1.2.0 - 2026-08-24

- 天秤マスタのCSV復元機能を追加。
- 添加材マスタのCSV復元機能を追加。
- CSV内の同一IDは上書き、未登録IDは追加するupsert方式。CSVにない既存データは保持。
- 復元前にCSVヘッダー、数値、単位、分類、有効状態、ID重複を検証。検証エラー時は保存を開始しない。
- UTF-8 BOM、CRLF/LF、引用符、引用符内のカンマ・改行・エスケープ引用符に対応するCSVパーサーを追加。
- 複数件のマスタ保存はIndexedDBの単一トランザクションで実行。


## Ver.1.1.0 - 2026-08-24

- 結果サマリーに添加量表示単位の切替を追加。
- `自動 / mg / g / kg / t` から選択可能。
- 対象は「総添加量」と、複数元素時の「各元素添加量」。
- 表示小数桁は、選択した単位へ換算した天秤分解能に合わせる。
- 選択した単位は設定として保存し、次回起動時にも維持。
- 「添加後総重量」は従来仕様どおり溶湯量と同じ単位を維持。

## Ver.1.0.10 - 2026-08-24

- 複数元素計算時、結果サマリーの同一グリッド内に各元素の「○○ 添加量」を直接追加する方式へ変更。
- 元素入力はHTMLの `select` のみに固定し、常時▼を表示するCSSへ変更。自由入力欄は使用しない。
- Ver.1.0.9以前の別枠サマリー描画を廃止し、表示漏れを防止。

## Ver.1.0.8 - 2026-08-24
- Androidなど画面幅760px以下でヘッダーのバージョン表示がCSSにより非表示になっていた問題を修正。
- モバイルでも「Ver.1.0.8」をアプリ名の下に常時表示。
- Service Workerキャッシュを Ver.1.0.8 に更新。

# Release Notes

## Ver.1.0.7 - 2026-08-21
- 2元素以上の配合計算で、結果サマリーに「元素別添加量」を確実に表示するよう修正。
- 元素別サマリーは「Cu 添加量」のように元素名と用途を明示。
- 配合計算の履歴自動保存処理を独立化し、計算完了後にIndexedDBへ即時保存するよう修正。
- 添加確認・歩留まり逆算・希釈計算も同じ履歴保存処理へ統一。
- 履歴保存後に履歴一覧を即時再読み込みし、設定・データ画面へ反映。
- 履歴保存だけが失敗した場合、計算結果そのものは破棄しないようエラー処理を分離。
- Service Workerキャッシュを Ver.1.0.7 に更新。

## Ver.1.0.6 - 2026-08-21

- 配合計算の「元素条件 > 元素」を添加材マスタの主元素から生成するドロップダウン選択に一本化。
- 元素条件では任意文字列を手入力できない仕様を明確化。
- プリセットや歩留まり実績に、現在の添加材マスタに存在しない元素が含まれる場合は、そのまま計算へ流さず再選択を要求。
- 元素未選択時の入力検証メッセージをドロップダウン選択前提へ変更。
- Service Workerキャッシュを Ver.1.0.6 に更新。

## Ver.1.0.4 - 2026-08-21

- 天秤マスタで分解能の単位を g / kg から選択可能に変更。計算内部では g に統一して処理し、推奨秤量値・秤量候補・総添加量は選択した天秤単位で表示。
- 元素入力を自由入力からドロップダウンへ変更。有効な添加材マスタの「主元素」から重複を除いて自動生成。
- 添加確認・歩留まり・歩留まり実績フィルタの元素リストも同じマスタ連動方式へ統一。
- 添加材マスタの「メーカー」欄を「備考」欄へ変更。既存メーカー値は備考が空の場合に移行。
- 既存天秤データは g 単位として自動移行し、Ver.1.0.3以前のバックアップとの互換表示も維持。
- Service Workerキャッシュを Ver.1.0.4 に更新。

## Ver.1.0.3 - 2026-08-21

- 計算過程（数式）を再設計。記号の意味を明示し、単一元素では「入力→理論添加量→天秤丸め→最終濃度」の4段階で表示。
- 数式表示の長大な生の内部小数を抑え、読みやすい桁数に整形。
- 配合計算の「添加後総重量」は、入力した溶湯量と同じ質量単位で表示。
- 添加確認の「添加後総重量」も、入力した溶湯量と同じ単位で表示。
- サンプル条件を Al 1900 kg / 1 g天秤 / Cu 0.1 ppm → 0.5 wt% / 歩留まり100% に変更。
- 結果サマリーの成功メッセージを「計算完了」のみに簡略化。
- Service Workerキャッシュを Ver.1.0.3 に更新。

## Ver.1.0.2 - 2026-08-21

- Ver.1.0.1で削除していた純元素プリセットを復元。
- プリセット名のみ `5N Cu / 5N Si / 5N Ti` から `Cu / Si / Ti` へ変更。
- 含有率は従来どおり 99.999 wt% を保持。
- Ver.1.0.1を一度起動してプリセットが削除済みの端末でも、Ver.1.0.2起動時に `Cu / Si / Ti` として自動復元。
- Ver.1.0.1のその他の修正（オンライン表示削除、四捨五入表記、天秤分解能連動、折り畳み式数式表示）は維持。
- Service Workerキャッシュを Ver.1.0.2 に更新。

## Ver.1.0.1 - 2026-08-21

- オンライン/オフラインのインジケーターを画面から削除。
- 丸め方式の表示名を「最近傍」から「四捨五入」へ変更（内部処理は従来どおり half-up）。
- 配合計算の結果サマリーと推奨秤量値を、選択した天秤分解能に対応する小数桁で表示。
- 初期添加材マスタから 5N Cu / 5N Si / 5N Ti プリセットを削除。既存インストールでも更新時に既定3件を削除。
- 配合計算に、標準モデルA・複数元素連立式・数値代入を確認できる折り畳み式「計算過程（数式）」を追加。
- Service Workerキャッシュを Ver.1.0.1 に更新。

## Ver.1.0.0 — 2026-08-21

初回実装。

### 計算
- 標準モデルAによる必要添加量計算
- 添加後濃度確認
- 添加歩留まり逆算
- 高純度Alによる希釈計算
- 複数元素の総添加重量連立補正
- wt% / ppm / ppb、mg / g / kg / t変換
- 高精度10進演算
- 天秤分解能に基づく秤量候補

### データ
- 添加材・天秤マスタ
- 歩留まり実績と採用/除外
- 歩留まり統計（件数、平均、中央値、最小、最大、標準偏差）
- 計算履歴
- JSONバックアップ/復元
- CSV出力

### PWA / UX
- PCサイドバー / スマートフォン下部ナビ
- オフラインヘルプ
- 初回チュートリアル
- Service Worker / manifest
- 初期サンプル添加材・天秤

### Ver.1.0の境界
- 複合母合金の副次成分連成計算はVer.2.0対象
- 製品規格判定はVer.2.0対象
- クラウド同期・ERP/MES連携は対象外
