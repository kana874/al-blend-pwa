# 計算仕様書 Ver.1.0

## 基本モデル

標準モデルAを使用します。

- `M`: 初期溶湯重量
- `C0`: 初期濃度（質量分率）
- `Ct`: 目標濃度（質量分率）
- `C1`: 添加後濃度（質量分率）
- `P`: 添加材中の目的元素質量分率
- `Y`: 添加歩留まり
- `x`: 添加材重量

添加後濃度:

```text
C1 = (M*C0 + x*P*Y) / (M+x)
```

必要添加量:

```text
x = M*(Ct-C0) / (P*Y-Ct)
```

歩留まり逆算:

```text
Y = [C1*(M+x) - M*C0] / (x*P)
```

希釈材濃度を `Cd`、必要希釈材量を `xd` とすると:

```text
xd = M*(C0-Ct) / (Ct-Cd)
```

## 複数元素

Ver.1.0では各添加材の副次成分によるクロス影響は扱いません。一方で、各添加材が最終総重量を増加させる影響は連立で厳密に考慮します。

各元素について `x_i = a_i + b_i*S`、`S = Σx_i` と変形して一括解を求めます。

```text
a_i = M*(Ct_i-C0_i)/(P_i*Y_i)
b_i = Ct_i/(P_i*Y_i)
S = Σa_i / (1-Σb_i)
x_i = a_i + b_i*S
```

## 数値精度

外部CDNを使わない要件に合わせ、BigInt係数＋10進スケール方式の同梱 `DecimalLite` を使用しています。四則演算は10進文字列を基礎に行い、除算は48桁の内部精度を標準とします。中間値は表示桁数では丸めません。

天秤分解能を適用する時のみ、指定分解能で四捨五入 / 切上げ / 切捨てを行います。


## Ver.1.0.6 データ・操作仕様

### 計算履歴

配合計算、添加確認、歩留まり逆算、希釈計算は、計算が正常終了した時点で `calculationHistory` に自動保存する。入力エラー等で計算が成立しない場合は履歴へ保存しない。

### 配合プリセット

`productRecipes` ストアを利用し、`kind = "blendPreset"` として保存する。保存対象は以下。

- 溶湯量の単位
- 天秤
- 丸め方式
- 元素
- 現在濃度の単位
- 目標濃度と単位
- 添加材
- 添加材含有率
- 歩留まり
- 歩留まり入力元

溶湯量の数値と現在濃度の数値は保存対象外とし、プリセット読み込み時には必ず空欄とする。

### マスタ外部出力

天秤マスタ、添加材マスタはUTF-8 BOM付きCSVとして個別に出力する。JSONバックアップには従来どおり全マスタ・プリセットを含める。

## Ver.1.0.6 元素選択仕様

- 配合計算の元素条件における元素は、添加材マスタの有効な登録データの「主元素」から抽出する。
- 抽出時は大文字小文字を無視して重複を除外し、UIではドロップダウンとして提示する。
- 元素条件では自由入力欄を設けない。
- 選択元素が現在の添加材マスタに存在しない場合、計算を停止し、ドロップダウンからの再選択を要求する。
- プリセット・歩留まり実績から配合画面へ反映する場合も同じ制約を適用する。

## Ver.1.0.7 履歴・複数元素サマリー仕様
- 配合計算で元素条件が2件以上の場合、各行の推奨秤量値を結果サマリーへ元素別添加量として表示する。
- 計算履歴は計算成功後に自動保存し、手動保存操作を要求しない。
- 履歴保存失敗は計算失敗とは分離し、計算結果は保持する。
- 履歴保存後は履歴一覧を再読込して画面表示と保存データを同期する。


## Ver.1.0.10 UI修正

- 元素条件の元素は、添加材マスタの有効レコードに登録された主元素を重複排除して生成する `select` のみで入力する。自由入力は禁止する。
- 複数元素（2元素以上）を計算した場合、結果サマリーに各元素の推奨秤量値を個別表示する。総添加量はその合計値として別表示する。
- GitHub Pages配布時は静的ファイルにアプリバージョンのクエリを付け、更新時の旧キャッシュ混在を避ける。


## Ver.1.1.0 結果サマリー表示単位

- 結果サマリーに「添加量単位」選択を設ける。
- 選択肢は `自動 / mg / g / kg / t` とする。
- 選択単位は「総添加量」と、複数元素時の各「元素名 添加量」に適用する。
- 推奨秤量値の内部値は変更せず、表示時のみ単位換算する。
- 小数桁は、天秤分解能を選択表示単位へ換算した値の桁数に合わせる。
  - 例: 1 g天秤でkg表示の場合は0.001 kg刻みなので3桁表示。
  - 例: 0.1 g天秤でkg表示の場合は0.0001 kg刻みなので4桁表示。
- `自動` は値の大きさに応じて mg / g / kg / t を選択する。
- 「添加後総重量」は本機能の対象外とし、溶湯量の入力単位と同じ単位で表示する。
- 選択値は `settings.summaryAdditionUnit` として保存する。


## Ver.1.2.0 マスタCSV復元仕様

### 共通

- 「設定・データ」の天秤マスタ、添加材マスタに個別の `CSV復元` 操作を設ける。
- 対象CSVは本アプリの各マスタ `CSV出力` で生成される形式を基準とする。
- UTF-8 BOM付き/なし、CRLF/LF、RFC4180相当の引用符付きフィールドを読み込める。
- CSVは全行を検証してからIndexedDBへ保存する。入力検証エラー時は1件も保存しない。
- 同一IDが既存マスタに存在する場合はCSV内容で上書きし、存在しないIDは追加する。
- CSVに存在しない既存マスタは削除しない。
- CSV内でIDが重複する場合はエラーとして復元を停止する。
- IDセルが空欄の場合は新しいIDを生成する。
- 復元完了後はマスタ一覧、元素ドロップダウン、添加材選択、天秤選択を即時再読込する。

### 天秤マスタCSV

必須列は `ID / 名称 / 分解能 / 単位 / 有効` とする。`分解能_g` 列は出力互換用であり、復元時の内部g値は `分解能 × 単位` から再計算する。

- `単位`: `g` または `kg`
- `分解能`: 0より大きい数値
- `有効`: `有効 / 無効` を標準とし、`true/false`, `1/0`, `yes/no` も受け付ける

### 添加材マスタCSV

必須列は `ID / 名称 / 分類 / 主元素 / 含有率_wt% / 備考 / 有効` とする。

- `分類`: `純元素 / 母合金`（`pure / master` も受け付ける）
- `主元素`: 空欄不可
- `含有率_wt%`: 0より大きく100以下
- `備考`: 空欄可
- `有効`: 天秤マスタと同じ判定


## Ver.1.3.0 計算履歴CSV復元仕様

### 対象CSV

アプリの「履歴CSV」出力で生成される以下の列を必須とする。

- `日時`
- `種類`
- `概要`
- `データJSON`

### 復元処理

1. CSV全体を解析し、必須ヘッダーを検証する。
2. `日時` が有効な日時であること、`種類` が空欄でないことを検証する。
3. `データJSON` をJSONオブジェクトとして解析する。
4. 全行の検証完了後にのみIndexedDBへ保存する。
5. 既存履歴と `日時 + 種類 + 概要 + データJSON` が一致する行は重複としてスキップする。
6. CSVにない既存履歴は削除しない。

CSV復元は履歴の再作成を目的とするため、CSVに履歴IDは含めず、復元時に新しいIDを発行する。
